name: DeepSeek V4 mini-oracle

on:
  push:
    branches: [main]
    paths:
      - 'tools/dsv4_mini/**'
      - 'tests/python/**'
      - 'tests/fixtures/dsv4-mini/**'
      - 'tests/fixtures/dsv4_cpu_ops_vectors.h'
      - 'tests/fixtures/dsv4_attention_numeric_vectors.h'
      - 'tests/fixtures/dsv4_sparse_index_vectors.h'
      - 'tests/fixtures/dsv4_attention_step_vectors.h'
      - 'requirements-dsv4-mini.txt'
      - 'Makefile'
      - '.github/workflows/dsv4-mini.yml'
  pull_request:
    paths:
      - 'tools/dsv4_mini/**'
      - 'tests/python/**'
      - 'tests/fixtures/dsv4-mini/**'
      - 'tests/fixtures/dsv4_cpu_ops_vectors.h'
      - 'tests/fixtures/dsv4_attention_numeric_vectors.h'
      - 'tests/fixtures/dsv4_sparse_index_vectors.h'
      - 'tests/fixtures/dsv4_attention_step_vectors.h'
      - 'requirements-dsv4-mini.txt'
      - 'Makefile'
      - '.github/workflows/dsv4-mini.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  cpu-reference:
    name: FP32 CPU oracle
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements-dsv4-mini.txt
      - name: Install CPU PyTorch and tests
        run: |
          python -m pip install --upgrade pip
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch==2.10.0
          python -m pip install 'pytest>=8,<10'
      - name: Syntax check
        run: python -m compileall -q tools/dsv4_mini tests/python
      - name: Regenerate canonical C operator fixture
        run: |
          python -m tools.dsv4_mini.make_c_ops_fixture \
            --output "$RUNNER_TEMP/dsv4_cpu_ops_vectors.h"
          cmp "$RUNNER_TEMP/dsv4_cpu_ops_vectors.h" \
            tests/fixtures/dsv4_cpu_ops_vectors.h
      - name: Regenerate canonical attention numeric fixture
        run: |
          python -m tools.dsv4_mini.make_attention_numeric_fixture \
            --output "$RUNNER_TEMP/dsv4_attention_numeric_vectors.h"
          cmp "$RUNNER_TEMP/dsv4_attention_numeric_vectors.h" \
            tests/fixtures/dsv4_attention_numeric_vectors.h
      - name: Regenerate canonical sparse index fixture
        run: |
          python -m tools.dsv4_mini.make_sparse_index_fixture \
            --output "$RUNNER_TEMP/dsv4_sparse_index_vectors.h"
          cmp "$RUNNER_TEMP/dsv4_sparse_index_vectors.h" \
            tests/fixtures/dsv4_sparse_index_vectors.h
      - name: Regenerate canonical attention step fixture
        run: |
          python -m tools.dsv4_mini.make_attention_step_fixture \
            --output "$RUNNER_TEMP/dsv4_attention_step_vectors.h"
          cmp "$RUNNER_TEMP/dsv4_attention_step_vectors.h" \
            tests/fixtures/dsv4_attention_step_vectors.h
      - name: Verify Make target uses isolated runner
        run: make -n test-dsv4-mini PYTHON=python | grep -F 'python tools/dsv4_mini/run_tests.py'
      - name: Unit and parity gates
        shell: bash
        env:
          PYTHONUNBUFFERED: '1'
        run: |
          set +e
          python tools/dsv4_mini/run_tests.py 2>&1 | tee "$RUNNER_TEMP/dsv4-mini-tests.log"
          rc=${PIPESTATUS[0]}
          if (( rc != 0 )); then
            detail=$(tail -n 80 "$RUNNER_TEMP/dsv4-mini-tests.log" | sed ':a;N;$!ba;s/%/%25/g;s/\r/%0D/g;s/\n/%0A/g')
            echo "::error title=Mini-oracle tests failed::$detail"
          fi
          exit "$rc"
      - name: Generate clean 130-token oracle
        run: |
          python -m tools.dsv4_mini.make_oracle \
            --config tools/dsv4_mini/tiny_config.json \
            --output "$RUNNER_TEMP/dsv4-mini-oracle.json" \
            --device cpu \
            --sequence-length 130
      - name: Validate generated summary
        run: |
          python - "$RUNNER_TEMP/dsv4-mini-oracle.json" <<'PY'
          import json
          import sys

          with open(sys.argv[1], encoding="utf-8") as handle:
              oracle = json.load(handle)
          assert oracle["parity"]["argmax_equal"]
          assert oracle["parity"]["mtp_argmax_equal"]
          assert oracle["parity"]["routes_equal"]
          assert oracle["parity"]["max_abs_error"] <= 2e-6
          assert oracle["trace"]["126"]["layers"][3]["attention"]["compressed_count"] == 0
          assert oracle["trace"]["127"]["layers"][3]["attention"]["compressed_count"] == 1
          print(json.dumps(oracle["parity"], indent=2, sort_keys=True))
          PY
